import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  isFetching: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  errorContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  errorMessage: {
    fontSize: 20,
  },
});
